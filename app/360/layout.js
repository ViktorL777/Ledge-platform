export const metadata = {
  title: '360° Assessment — Ledge',
  description: 'Measure your leadership competencies with validated assessment templates, AI-powered questionnaire builder, and instant visual reports.',
};

export default function Layout({ children }) {
  return children;
}
